[project]
name = "autplay-local-music-acquisition"
version = "0.1.0"
description = "Portable local-only Jamendo, Hitmo, yt-dlp, and Yandex acquisition contours"
requires-python = ">=3.12,<3.15"
dependencies = [
    "playwright==1.60.0",
    "requests==2.34.2",
    "yandex-music-downloader @ git+https://github.com/llistochek/yandex-music-downloader.git@9d33d6aaefae3cb882d02822e59cf0796c33a651",
    "yt-dlp[default,pin]==2026.8.19",
]

[project.scripts]
local-music-acquire = "local_music_acquisition.cli:main"

[build-system]
requires = ["hatchling==1.28.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/local_music_acquisition"]

[tool.hatch.metadata]
allow-direct-references = true

[dependency-groups]
dev = [
    "mypy==2.3.0",
    "pytest==9.1.1",
    "ruff==0.16.2",
]

[tool.uv]
required-version = "==0.12.3"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.mypy]
python_version = "3.14"
strict = true
files = ["src"]

[[tool.mypy.overrides]]
module = ["ymd.*", "yandex_music.*", "yt_dlp.*"]
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "RUF", "SIM", "UP"]

[tool.ruff.format]
line-ending = "lf"
quote-style = "double"
