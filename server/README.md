# AutPlay server skeleton

This directory contains only the typed P01 package boundaries and dependency lock. It has no API endpoint, persistence mapping, migration, worker, or feature logic.

Use the canonical repository commands in the root [`README.md`](../README.md).
