# AutPlay Risk Register

P01-P11 provide the executable foundation through the deterministic CPU recommendation baseline.
P13 verifies the trusted-local Hybrid Wave lifecycle, authorization, recovery and timing policy.
P12 verifies isolated GPU enrichment contracts, device selection, bounded failure and versioned
rollout without activating a model. P14 verifies the local CPU release package, restore, security,
dependency, large-search and physical Samsung A55 gates.

| ID | Risk | Likelihood | Impact | Mitigation / gate | Evidence / owner | Status |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | Data loss from destructive or incompatible PostgreSQL/Room migration, including IDs, playlist order, Journal events or tombstones | Medium | Critical | No destructive fallback; named migrations; supported-version fixtures; upgrade/downgrade/restore drills | Alembic through `0015`; Room v1-v11 preservation including additive server-feature projections; P14 independent PostgreSQL/Vault restore, checksum and corruption-injection evidence | MITIGATED for the tested RC generation; any production topology still needs its own pre-rollout restore drill |
| R-002 | False identity merge combines live/remix/edit/remaster or different recordings | High without gates | Critical | Unresolved-first model; hard conflicts; immutable evidence/history; benchmark-gated auto-match; ADR-019 exact-byte eligibility fails closed and never merges or mutates owner projection | P02 immutable ledger/F-016 enforcement; ADR-019/ADR-023; P06 exact-byte gates; P10 hard-negative/hash report, T0-T4 all-shadow real-PG evidence and explicit review; A-024/A-025 PASS | MITIGATED for P10; auto-match remains disabled until the complete production activation gate |
| R-003 | Sync duplicate/reorder/conflict/tombstone handling loses offline intent or advances cursor partially | Medium | Critical | P04 language-neutral contract and vectors; transactional Journal; idempotent inbox; cursor only after full apply; visible conflicts | P04 51-test gate; ADR-018/ADR-022; P09 real-PostgreSQL duplicate/concurrency/compaction/bootstrap tests and API 26 coordinator/reset/profile-isolation suite; A-007/A-018-A-022 PASS | MITIGATED for the P09 runtime; production-scale soak and disaster recovery remain P14 |
| R-004 | Vault crash, corrupt bytes, path escape, duplicate commit or unsafe GC makes partial/corrupt audio visible or deletes the last copy | Medium | Critical | Same-filesystem staging, bounded validation, SHA-256, no-overwrite hard-link publication/fsync, immutable CAS, O(1) verified size/mtime stream proof, quarantine and full-hash reconciliation | `HANDOFF_P06.md`; real PostgreSQL crash/concurrency/expiry/reconcile tests; filesystem safety tests; pinned-image hostile-media smoke; A-011-A-013 PASS | MITIGATED for P06 local filesystem backend; backup/restore and production NAS qualification remain P14 |
| R-005 | Revoked/missing Android MediaStore/SAF URI is treated as deletion and destroys user intent | High over device lifetime | High | Store URI separately; validate permission/readability; retain `UserTrackRef`; fallback to Vault; rescan/reconcile | P05 missing-URI round-trip; P07 real denied-provider and missing-import intent/FTS tests; P08 resolver, revoked-provider and real Vault fallback tests; A-015 | MITIGATED for P08 playback fallback; future rescan/repair UX remains |
| R-006 | External provider/API/legal change breaks recovery or encourages unauthorized acquisition | Medium | High | Capability-declaring adapters; user-owned/official export first; bounded contract fixtures; no DRM/secret scraping; provider not source of truth | P10 generic/local adapter manifests and fixtures; live-provider choice explicitly deferred; P14 review | MITIGATED for P10 inputs; any live provider still requires an explicit legal/policy decision |
| R-007 | GPU OOM, driver/runtime failure, malicious/unlicensed model or supply-chain drift affects core readiness | Medium | High | Separate optional project/process; capability-based device selection; exact ONNX/CUDA lock; approved model registry/hash/license; one heavy job; bounded batch reduction/checkpoint; CPU baseline remains available | ADR-025; A-029/A-031 PASS; concrete ONNX worker, handler and real-PG restart evidence; laptop UNAVAILABLE probe; real RTX OOM/metrics and P14 SBOM remain | PARTIALLY MITIGATED; activation stays blocked until real RTX approval |
| R-008 | Backup archive exists but cannot restore a consistent PostgreSQL/Vault generation | Medium | Critical | Generation manifest, checksums, isolated restore and reconciliation; quarterly target drill; RPO/RTO evidence | A-034; `P14_BACKUP_RESTORE_2026-08-17.json` restores a custom dump and immutable blob into a second isolated project and detects injected corruption | MITIGATED for local quiesced full-generation backup; production target/retention requires deployment approval |
| R-009 | Token, private URL, user path, raw payload or credential leaks through logs/export/diagnostics | Medium | Critical | Typed secret handling, recursive allowlist/redaction, bounded structured logs, secret scanning, safe diagnostic bundle | A-035; zero-finding production-source scan, targeted runtime/import/log negatives and path-scoped disposable-credential allowlist | MITIGATED for current RC source/runtime surface; a selected hosted Git/deployment path still needs historical scanning |
| R-010 | Broken object authorization exposes another user’s library, playlist, job, recommendation or Vault bytes | Medium | Critical | Session-bound principal reload, owner-scoped application/repository queries, negative cross-user tests, stream authorization independent of hash/URL | A-036; targeted 66-test security suite plus canonical real-PG owner/FK/API cases across current resources | MITIGATED for current RC resources; every future resource must repeat the owner-negative gate |
| R-011 | Android or personal-server performance misses interactive/streaming SLOs at target scale | Medium | High | Named datasets/hardware, bounded queries, pagination, resource priorities, p50/p95/p99 reports; Samsung A55 and minimum-tier checks | A-037/A-038; 100k/120-query PostgreSQL search p95 6.403 ms; API 26 10k FTS p95 12.555 ms and 1k playlist p95 11.876 ms; Wave bounds and physical SM-A556E smoke pass | PARTIALLY MITIGATED; production concurrency/soak and physical-device performance profiling beyond the declared smoke remain open |
| R-012 | Room 3/JDK/Kotlin/KSP/AGP/Gradle/Media3/WorkManager combination is incompatible on minSdk/Samsung A55 or R8 | Medium | High | P01 pins and proves JDK/Gradle/AGP/Kotlin/Compose/minSdk shell; Room/KSP/SQLite/WorkManager/DataStore gate before schema v1; Media3 device gate in P08 | Android host lint/unit/debug/release/R8, 82/82 connected API 26 tests and physical SM-A556E dev-signed install/background/process restart pass | MITIGATED for the tested RC pins and declared A-038 smoke |
| R-013 | Sync semantics remain underspecified until Sync Protocol v1 is created | Certain before P04 | Critical for sync implementation | Apply accepted P00-D006 Variant A/R1; freeze ordering, ACK, cursor, tombstone, bootstrap, conflict, compatibility and limits before engines | `AutPlay_Sync_Protocol_v1.md`; fourteen generic/specialized schemas; OpenAPI 3.1; 51 contract tests; ADR-018/ADR-022; `HANDOFF_P04.md` and `HANDOFF_P09.md` | MITIGATED; contract and both P09 engines are executable |
| R-014 | Executable P02 persistence could drift from the accepted immutable identity decision/evidence/history contract | Possible | High | Enforce accepted ADR-015 and synchronized Track Identity/ER/reference DDL through real PostgreSQL inventory, invariant and migration tests | `HANDOFF_P02.md`; A-003/A-004 PASS; exact 57/53/13/40 and zero drift | MITIGATED in P02; future migrations must retain the gate |
| R-015 | Phase graph ambiguity allows P14 to imply unimplemented P12/P13 capabilities | Medium | High | Require explicit approved deferral and visible release labeling; formalize P00-D005 before P14 | ADR-027, P12/P14 progress rows, matrix, RC checklist and release notes explicitly isolate A-030; P13 and A-038 PASS | MITIGATED; GPU remains inactive and only A-030 carries the approved deferral |
| R-016 | Development/CI toolchains or host behavior drift from the validated baseline and Linux production | Low locally; Medium cross-platform | Medium | Exact JDK/SDK/Gradle/uv/container gates and clean API/worker smoke; platform-neutral Windows/Linux/macOS CPU + Linux Android CI plan; require cold hosted jobs when provider exists | P03 PowerShell/Git Bash 298-test gates and Linux-container runtime smoke; ADR-013; `VERSIONS.md`; `CI_PLAN.md` | OPEN pending hosted cross-platform evidence |
| R-017 | Cloud-synced workspace semantics interfere with file locks, Gradle caches or filesystem atomicity experiments | Medium | Medium | Generated caches ignored; P01 build and P02 database lifecycle are proven in the current synced/non-ASCII path; run Vault atomicity tests on a named local filesystem matching intended deployment | P01/P02 handoffs; P06 filesystem report | OPEN |
| R-018 | Lease expiry or worker loss causes a stale attempt to publish duplicate or terminal side effects | Medium | High | Atomic `SKIP LOCKED` claim/attempt creation; DB-authoritative lease time; `attempt_no` fencing on heartbeat/checkpoint/terminal updates; bounded deterministic retry; idempotent external publication | P03 job gates; P06 Vault retry windows; P10 deterministic job/row IDs, bounded checkpoints and real-PG replay/resume evidence | MITIGATED for `vault.ingest/v1` and `library.import/v1`; future handlers must prove their own effects |
| R-019 | Local Codex automation overwrites user work, bypasses phase blockers, leaks task payloads, or enters an unbounded review/fix loop | Medium without gates | High | Protected-branch and dirty-tree refusal; destructive-request gate; argument-vector commands; atomic/resumable state; allowlisted redacted events; dependency-aware backlog; read-only reviewer; bounded iterations; canonical final checks | Harness unit/CLI/canonical evidence in `HANDOFF_HARNESS_V1.md` | MITIGATED for harness v1; real SDK turns still require operator credentials/service availability |
| R-020 | Recommendation feedback is incomplete, double-counted or irreproducible because generation is treated as impression, causal attribution is guessed, or model/config versions are detached from input snapshots | High without early contract | High | P04 actual-impression and attribution schemas/hash vectors; one owning domain event; P08 captured queue/session owner and immutable attribution; P09 exactly-once interaction projection; P11 immutable pipeline/seed/watermark/catalog snapshots and replay/evaluation gates | ADR-017/ADR-024; P04 contract tests; P09 causal/semantic race evidence; P11 retained snapshot, replay, provenance, stable Room presentation and evaluator tests | MITIGATED through P11 CPU generation/replay; future P12 components must preserve this boundary |
| R-021 | Stop-hook replay starts a successor twice, or a stale/corrupt state bypasses a phase gate | Low with gates | High | Separate initialized state; no model-text completion signal; mandatory real gates; atomic pre-output claim; `stop_hook_active`; consumed-edge ledger; fail-closed missing/corrupt state; no automatic replay after state loss | Phase-pipeline simulations, strict lint/type/unit suite, current-structure validation and canonical gates in `HANDOFF_HARNESS_PHASE_PIPELINE_V1.md` | MITIGATED for P04 -> P05; crash after claim can lose continuation by design, but cannot duplicate it |
| R-022 | Mobile clock asymmetry, dropped live hints, revoked media access or host loss desynchronizes Wave or broadens access | Medium | High | REST/PostgreSQL sequence truth; snapshot-first reconnect; per-device P06 authorization and expiring final-ready gate; monotonic schedule; strict clock eligibility; bounded drift correction; durable transfer/close/expiry commands | ADR-026; real-PG Wave lifecycle/ACL/start tests; runtime WS gap/revoke/source tests; API 26/JVM timing evidence; A-032/A-033 PASS | MITIGATED for trusted-local single-API P13; public Internet/TLS, cross-instance fanout and physical multi-device WAN qualification remain deferred |
| R-023 | A broad Android UI exposes fake success, raw paths/private configuration, inaccessible controls or phone-only layouts that diverge from delivered application contracts | Medium | High | Typed destinations; compact/medium/expanded breakpoints; contract-backed actions; readiness-gated Wave start; bounded off-main SAF tree import; secret-free settings transfer; active-queue feedback and stale-callback guards | `HANDOFF_FRONTEND_M1.md`; `HANDOFF_FRONTEND_M2_SERVER_SURFACES.md`; host unit/lint/build and 89/89 physical Samsung connected tests | MITIGATED for tested phone/server surfaces; expanded multi-pane and full item-level UX remain follow-up work |
| R-024 | Optional-server UI accidentally makes local actions network-dependent, persists sparse server pages as canonical library data, or loses durable upload/import state on process death | Medium | High | Application ports; no Compose networking; ephemeral server library pages; additive Room projections only for remote jobs/upload intents/recommendation snapshots; bounded WorkManager retries | Room v10-v11 migration/DAO tests; transport and worker tests; live import restart smoke; physical offline/unavailable UI tests | MITIGATED for M2; real Vault byte E2E awaits an eligible synced local recording |

## Review rule

- Critical/high data-loss or object-authorization risk blocks P14 release candidacy.
- A risk moves to mitigated only with a linked executable test/report, not a prose claim.
- Accepted residual risk or deferral requires explicit approval and remains visible in the release handoff.
