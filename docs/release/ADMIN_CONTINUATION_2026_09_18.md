# Admin/accounts continuation: updated 2026-09-19

Unified seven-criterion status and the exact remaining physical boundary are indexed in
[the current goal record](ADMIN_UNIFIED_GOAL_2026_09_19.md).

Latest local continuation: retained training now composes current authority, historical
preparation through a separate connection, immutable input/output inventories, global
byte/process admission, bounded cancellation, exact process-tree exit and durable cleanup.
The production Sona serving entry point reads the exact current publication tuple before
model load. The trusted [offline execution drain](ADMIN_OFFLINE_EXECUTION_DRAIN_2026_09_19.md)
atomically resolves restored open reservations only after real host-empty proof, then routes
training through ordinary inventory cleanup. Production stays disabled pending physical
target acceptance.

Latest slice: [exact deletion resolution and retained early intent](ADMIN_DELETION_RESOLUTION_2026_09_19.md).
Expired originals that were never attempted receive an immutable exact negative;
Android validates it and durably clears only its identical journal. Acceptance and
cancellation intent precede PG commit and survive old restores/rollback ambiguity.
Offline first provisioning samples PG cutover after all old acceptors/transactions
stop, with a 240-second readiness fence. Current-schema readiness now expects 0057.
Local evidence covers 57 distinct PG cases, 29 journal/startup, two contract,
10 readiness/metadata, one full lifecycle, 18 Android JVM and 11 GPU cases. The final
APK/lint succeeded with standard 2 GiB heap; 17 server files pass Ruff/format/mypy.
Full seven-criterion goal remains active because real Windows Hello/A55/private-network,
joint target measurements and an isolated restored-backup rehearsal are still required.

Preceding slice: [power-loss audit and independent consent restore fence](ADMIN_TRAINING_RESTORE_2026_09_19.md).
The disposable PostgreSQL cluster recovered WAL after the laptop lost power; saved
Git/Android evidence survived and current PG/CPU/ONNX checks passed again. Every new
training authority now requires the exact latest independent consent intent and PG
receipt. Startup reconciliation privatizes unmatched grants and retires unfinished
runs without changing completed-model serving. Bounded review is closed after fixing
run-lock order and reserving withdrawal capacity. The later continuation advances the
schema to 0057 and composes retained preparation/process/cleanup. Target-device acceptance
remains open; production stays disabled.
Final affected server batch: 134 passes in 66.13 s; PG/CPU/ONNX/consumer: three
passes in 41.74 s; GPU startup: two passes in 1.75 s. The broader GPU typing check
now passes after the subsequent sync typing/query-budget fixes; 29 real PG sync
cases pass with the original catalog query bound.

Preceding slice: [live trainer, checkpoint seal and exact publication/consumer gate](ADMIN_TRAINING_EXECUTION_2026_09_19.md).
Owner-derived training requires exact current inputs and bounded batch checks. Migration
0056 seals the live trainer's checkpoint digest; export/publication and serving require
that seal and the exact artifact dependency tuple. Synthetic relabelling, checkpoint
transplantation and rehashed export replacement are rejected. Real PG/CPU/ONNX/consumer:
three passes; trainer/approval: 27 distinct; registry/policy/metadata: 39 distinct;
affected lifecycle/drift/PUBLIC: four; worker checks: 14 passes/one unavailable symlink.
Bounded review is closed. The later local-bridge closure sets the current head to
**0060_local_bridge_authority**, 170 tables, 1921 columns and 156 explicit indexes. Retained preparation/process/cleanup composition
is implemented; actual target-device acceptance remains open and production stays disabled.

Preceding slice: [training registry foundation](ADMIN_TRAINING_REGISTRY_2026_09_18.md).
Migration 0055 adds immutable participant/run/publication authority and atomic terminal
invalidation/cleanup claims; it does not compose training-root bytes/process cleanup.
Registry/policy/metadata: 35 passes; affected live drift/index/ACL/rollback: four;
last-OWNER RR: one. Bounded review is closed. Its historical head: **0055_training_work**,
167 tables, 1878 columns, 155 explicit indexes.

The preceding [shared-training policy and Android controls](ADMIN_TRAINING_CONSENT_2026_09_18.md)
adds current caller-owned consent and exact receipts; production
enablement is rejected until training execution/cleanup enforcement is complete.
Its policy/migration batch passed 35 cases; runtime/settings: 57; capability: six;
Android now covers 48 distinct affected JVM cases and three UI cases plus both APK builds.
Bounded reviews are closed.
Retained training preparation/process/cleanup is implemented in the later continuation;
the ordinary consent restore fence is implemented in the latest slice above.

Android deletion/cancellation is also locally implemented and reviewed:
[verification and limits](ADMIN_ANDROID_DELETION_VERIFICATION_2026_09_18.md).
Its final affected groups cover 96 distinct JVM cases and four new UI cases; six
unchanged recovery UI cases passed earlier. Negative resolution is implemented in
the latest linked slice; physical/server acceptance remains open.

Migrations 0052/0053 implement suspension, cancellation, last-OWNER protection,
final PostgreSQL purge and independent ledger/startup gate, as recorded in
[the server deletion evidence](ADMIN_ACCOUNT_DELETION_2026_09_18.md).
Their final combined regression passed 131 cases. The later offline execution drain closes
the local implementation gap; none of these slices substitutes for deployment acceptance.

Latest Android status: [recovery implementation and verification](ADMIN_ANDROID_RECOVERY_VERIFICATION_2026_09_18.md).
It supersedes the [interrupted Android handoff](ADMIN_ANDROID_RECOVERY_HANDOFF_2026_09_18.md):
both review findings and new-account TXT setup are implemented and reviewed; 83 distinct
JVM cases, both APK builds, six emulator UI cases and separate final lint pass.

The user resolved the last-OWNER deletion question: deletion of the last ACTIVE,
nondeleted OWNER is forbidden. See ADR-053(6); migration 0052 now enforces this
transactionally, including concurrent requests and stale REPEATABLE READ snapshots,
without auto-promotion or relaxed bootstrap.

This is the current local handoff for a fresh task, prepared after the user asked
whether to move chats. The full goal is unfinished. Earlier 50-55% estimates were informal and did not
represent measured acceptance. The user objected to unchanged percentages and
excessive focus on infrastructure. Report completed functions and remaining work;
after the metadata block, prioritize TXT/manual account recovery before returning
to the remaining maintenance/path backlog. Preserve all seven goal criteria.
The user asked to reduce
repeated test runs: implement complete functional blocks, use early targeted checks
only for foundational migration/race risks, then run one affected regression batch.
Repeat broad checks only when a new failure or implementation change justifies it.

## Workspace and authority

- Continue in the same local repository: `D:/AutPlayProd/AutPlay`, under saved project
  `D:/AutPlayProd`. Branch: `codex/readme-current-state`; HEAD:
  `860511aae9f1fcdb98b7b9b944f97ef27929fb8b`.
- The working directory contains extensive uncommitted and untracked implementation,
  including adjacent music/metadata work. It is authoritative. A clean worktree or
  HEAD alone does not contain this implementation. Preserve all adjacent changes.
- Local implementation and disposable checks are authorized. Commit, push, deployment,
  production migrations and real credentials require their existing separate authority.
- The user disabled `skill-state-workflow`. Do not invoke it, run its state guard,
  or update `.codex-state`. The historical 2026-09-16 checkpoint's state-workflow
  resumption instructions are superseded by that user decision and this handoff.
- Apply the repository `AGENTS.md` and local `autplay-development` skill. Use a bounded
  independent read-only review for substantial changes. Do not interpret historical
  milestone notes as new authorization or replay the P00-P14 build sequence.

## Full objective to retain

Implement the accepted Admin/account plan in full: four Admin sections and WebAuthn;
QR self-service for all account roles; one-time TXT/manual account recovery with
complete authority revocation; reversible deletion for 30 days with backup protection;
revocable shared-training consent; editable defaults 5/2/2 plus measured global
admission on every relevant byte/worker path, retaining capacity until exact process
exit. All seven `objective_revision=2` acceptance criteria require current PostgreSQL,
server, Android and browser evidence, revocation/quota races, closed independent
review, actual target devices and measurements. The completed slices below do not
replace or narrow this goal.

Read these requirement sources as needed:

- `docs/design/explorations/AutPlay_Admin_And_Account_Onboarding_Draft_v1.md`
- `docs/adr/ADR-053-admin-and-account-self-service.md`
- `docs/design/AutPlay_Admin_Web_Security_Contract_v1.md`
- `docs/design/AutPlay_Resource_Admission_Contract_v1.md`
- `docs/design/explorations/AutPlay_Resource_IO_Stop_Amendment_v1.md`
- `docs/design/AutPlay_Resource_Measurement_Report_v1.md`

## Verified local implementation and limitations

Earlier scoped evidence covers the four Admin hubs, browser passkeys, self-device
pairing server/Android protocol, admission/quota control, retained upload/stream
processes and bounded HTTP transport handling. These have not passed the full
target-device/private-network acceptance. Details:
`docs/release/ADMIN_RESOURCE_HTTP_2026_09_16.md` and
`docs/design/explorations/AutPlay_Admin_Implementation_Checkpoint_2026_09_16.md`.
The latter's five HTTP findings were subsequently closed in the HTTP release record;
its old port, migration head and state-workflow instructions are historical.

Current worker evidence is in `docs/release/ADMIN_RESOURCE_WORKERS_2026_09_16.md`.
Controlled Internet/A1 paths have Windows process-tree retention, resource waiting,
pre-handoff staging ownership, scratch cleanup and a durable singleton maintenance
process. CPU Jamendo composition now uses the controlled handler when configured; production maintenance scheduling remains off.
Linux provider-tree containment is still open.

An earlier completed slice is orphan CAS retirement, migration
`0043_orphan_object_claim`. It claims only keys without ANY object metadata or local
replica metadata, serializes with CAS preparation, preserves bytes in quarantine,
and requires exact successful process exit before claim completion. Registered
STAGING/QUARANTINED objects remain protected after job expiry or detach. A concurrent
second maintenance execution prevents premature completion; old receipts cannot
retire a later publication. New implementation entry points:

- `server/src/autplay/application/orphan_object_retirement.py`
- `server/src/autplay/adapters/postgresql/orphan_object_retirement.py`
- `server/src/autplay/adapters/postgresql/provider_maintenance.py`
- `server/src/autplay/runtime/provider_maintenance.py`
- `server/src/autplay/adapters/filesystem/provider_maintenance_child.py`
- `server/src/autplay/adapters/filesystem/orphan_object_retirement.py`
- `PostgresVaultRuntime.acquire_sha_lock` in `adapters/postgresql/vault_runtime.py`

Latest evidence: the combined PostgreSQL run passed 73 cases; all 14 orphan cases
passed after a follow-up test review fix and an added lost-commit-response case.
Together the unchanged and final passing sets cover 74 distinct PostgreSQL cases,
including existing provider maintenance/scratch/Vault, clean lifecycle, all 43
adjacent migration pairs, live Alembic drift and privilege checks.
Windows: 18 passed, three symlink cases
unavailable on this host. Linux: all 50 passed, including all three symlink cases
and actual isolated child launches. Formatting, Ruff and strict mypy passed on
23 changed Python files; the changed test passed these checks again after review.
`git diff --check` passed. Bounded implementation review found no defects, and a
follow-up test review finding was fixed and closed. Tests were run by the
implementing agent. No full acceptance or production claim is made.

## Progress in the continuation task

The next local slice is now implemented and independently reviewed:
`docs/release/ADMIN_VAULT_INVENTORY_2026_09_18.md`. It adds a bounded live filesystem
cursor, positive-only PostgreSQL classification, and isolated orphan-service
orchestration with retained batch/canonical-claim retries. Final evidence is 27
PostgreSQL passes (13 new inventory + 14 existing orphan), 21 Windows passes with
seven unavailable symlink cases, and all 15 Linux scanner cases passing. Ruff,
formatting and strict mypy passed on six files; bounded read-only review is closed.

The follow-up retained scanner is also implemented and reviewed, using the same
durable singleton across pages with migration `0044_inventory_maintenance` (the
head for that slice). Its release section records 88 distinct PostgreSQL cases,
41 Windows passes with seven unavailable symlink cases, all 48 Linux cases, and
Ruff/format/strict mypy on 19 affected Python files. Tests include actual scanner
children, blocked pipes/DB, idle expiry, delayed exit, and close-during-decode.

The next slice adds explicit missing-both orphan resolution, migration
`0045_orphan_missing` (head for that slice), and distinct `MISSING`/`RETIRED` receipts.
It checks the exact source/quarantine paths and stable safe directory identities
in an isolated check-only child, then requires acknowledged zero exit. Old claim
replays do not touch later publications. Both design and implementation reviews
are closed. Current evidence: **108 PostgreSQL passes**, **50 Windows passes with
10 symlink skips**, **60 Linux passes**, and Ruff/format/strict mypy on 18 files.
See the detailed release record for reproducible groups and evidence limits.

The sequential reconciliation CLI now replaces the legacy destructive DB method
and caller-owned Vault UoW. It retains the scanner across bounded pages, waits for
acknowledged exact exit, and drains canonical durable claims, including explicit
MISSING resolution. `--apply --drain-only` resumes claims after restart without a
new scan. CLI output explicitly declares `unregistered_cas` scope and pending
tracked reconciliation; `--limit` is now a page/query budget of 1..100, not total
work per invocation. No production maintenance was activated and `.codex-state`
was untouched. See the latest section of the detailed release record.

Current composition evidence covers 59 distinct Windows PostgreSQL cases, with
all 18 orchestration cases passing after final review fixes; the Windows
CLI/application/CPU group passed 24. Linux passed all 65 cases (59 PostgreSQL + six
CLI validation), including a fresh actual CLI process. Ruff/format/strict mypy
passed on 10 affected Python files and `git diff --check` passed. Independent
review is closed; no full-goal or production acceptance claim is made.

## Next concrete work

The HTTP upload creation window is now closed; see
`docs/release/ADMIN_UPLOAD_OWNERSHIP_2026_09_18.md`. POST/replay commits metadata
without filesystem access, and the first admitted PATCH creates staging only
inside the registered RUNNING process under the upload row lock. A missing
nonzero prefix cannot be recreated. The child checks the full remaining-size
watermark and durably publishes directory entries before acknowledgement. The
real adapter rejects PATCH without a coordinator before reading its body.

Final Linux evidence is **109 passed** (56 PostgreSQL + 53 child/filesystem/
application/API), including symlinks and actual child processes. Windows passed
**69** unit/API/CPU cases with two symlink skips; after the final durability fix
the affected upload PostgreSQL group passed **16** again. Ruff/format/strict mypy
passed on 16 files and `git diff --check` passed. Independent review is closed.
There was no migration, deployment or production enablement.

Terminal device upload cleanup is now implemented with migration
`0046_upload_cleanup`, described in the follow-up section of
the upload ownership release record. Cancel/expiry atomically queue immutable
cleanup claims; HTTP performs no filesystem work. `vault-upload-cleanup` drains
those claims and backfills historical eligible uploads through the common retained
maintenance child. Completion requires exact acknowledged zero exit, stable safe
directories and no unclosed writer. Both cancel/ingest lock winners and delayed
exit acknowledgement are covered; SQL guards prevent actor changes or backwards
writer transitions. The lock race also found and fixed stale ORM state after
waiting for FOR UPDATE. Independent read-only review is closed. Final run details
remain in the release record; no scheduler or production migration was activated.

1. Recovery, Android deletion/cancellation and account-policy consent controls are
   locally implemented and reviewed; evidence and limits are linked at the top.
   Continue target acceptance. Current head
   is `0059_training_publication_seal`. Ordinary consent restore fencing is implemented.
   Training now has durable global measured admission, a five-second database-clock
   authority lease, retained child launch, immutable-root preparation, exact retained-capacity
   release and cleanup composition. See
   [the training execution record](ADMIN_TRAINING_EXECUTION_2026_09_19.md).
   Preserve all seven goal criteria; reuse passing checks
   whose inputs did not change rather than accumulating repeated test counts.
2. The contained metadata worker is now implemented and independently reviewed.
   Its baseline was `0050_metadata_execution`: 157 tables, 1789 columns, 150 indexes;
   the deletion/purge migrations above are now the head.
   Its pinned source/account/job authority, durable provider pacing, direct CAS reads,
   pipe-only artwork and optional private AcoustID POST share retained execution and
   the measured global internal budget. New admission requires eleven-path report v3;
   Python/SQL reject evidence downgrade. Verification: 162 distinct Linux passes,
   two Windows Job Object passes, strict checks on 35 files (27 typed source files),
   live migration drift/ACL checks and closed read-only review. See
   [the metadata record](ADMIN_METADATA_OWNERSHIP_2026_09_18.md).
   WORK/finalized cleanup and controlled Jamendo remain wired into worker_cpu.py;
   their earlier verified scope is in the ingest record. No production enablement.
3. Every currently enabled byte/worker path is named by report v3. Tracked-CAS integrity
   reconciliation remains a separate inactive future capability and must extend measured
   coverage before it can be activated. Do not invent a TRANSFER permit or derive measured
   capacity from process-local concurrency.
4. Rehearse the implemented offline restore closure/drain path against an isolated real
   backup and validate the explicit supervisor-stop boundary on the target host. Final
   browser/device/private-network acceptance and trusted joint measurements remain open.

Linux containment now has a real delegated cgroup v2 adapter and isolated proof:
24 Linux cases (17 actual cgroup + 7 PostgreSQL) passed in 53.15 s, with the test
runner at UID/GID 10001 and CapEff=0. The same seven database cases plus fifteen
Windows Job Object cases passed on Windows (22 passed, 44.79 s). Review is closed.
See [the containment record](ADMIN_LINUX_CONTAINMENT_2026_09_18.md) for setup,
assumptions and the pre-GO/PID-reaping and failed-DB-acknowledgement races.
Ordinary Docker cgroup mounts remain read-only. A disposable container with a
private cgroup namespace and SYS_ADMIN only for setup provided a delegated subtree;
the implementation itself never mounts or delegates. Production delegation and real joint measurements remain pending. CPU ingest/cleanup
and Jamendo composition now use retained children and separate control pools.
The metadata and training workers now use retained children as described above; every
enabled byte path is covered by report v3. Real credentials, laptop/A55 checks and physical
measurements require user participation under the
[target acceptance runbook](ADMIN_TARGET_ACCEPTANCE_2026_09_19.md). M52 is not a substitute
for A55.

## Reproduce the latest checks

Use locked `uv` dependencies from this repository. PostgreSQL currently runs in the
owned disposable container `autplay-admin-20260916-postgres-1`, loopback port 3399
(verified on this date). Recheck Docker before use. Tests create isolated databases;
do not migrate a production database.

```powershell
$env:AUTPLAY_TEST_DATABASE_URL='postgresql+psycopg://autplay:autplay_dev_only@127.0.0.1:1520/autplay?connect_timeout=3'
uv run --project server --frozen python -m pytest -c server/pyproject.toml server/tests/postgresql/test_orphan_object_retirement.py server/tests/postgresql/test_provider_maintenance.py server/tests/postgresql/test_vault_runtime.py server/tests/postgresql/test_metadata.py server/tests/postgresql/test_migrations.py server/tests/postgresql/test_migration_close_gates.py server/tests/postgresql/test_resource_admission_schema.py server/tests/postgresql/test_provider_scratch.py -q -x --tb=short -W error::sqlalchemy.exc.SAWarning
uv run --project server --frozen python -m pytest -c server/pyproject.toml server/tests/test_orphan_object_retirement.py server/tests/test_provider_maintenance_child.py -q -x --tb=short
```

Linux proof uses `autplay-acquisition:perf-gate-20260914`, read-only mounts for
`server/src` at `/source` and `server/tests` at `/proof`, and Python
`/opt/acquisition/.venv/bin/python`. Install the pinned test dependencies and write
`autplay_source.pth` into its site-packages pointing to `/source`: isolated `-I`
children ignore PYTHONPATH and the image has no installed AutPlay package.
The 50-case set is `test_orphan_object_retirement.py`,
`test_provider_maintenance_child.py`, `test_provider_filesystem.py`, and
`test_provider_scratch.py`, using `-c /dev/null -p no:cacheprovider`.

At this checkpoint the reported Windows, Linux and PostgreSQL test sessions have
terminated successfully. No runtime child was left pending by those tests.
